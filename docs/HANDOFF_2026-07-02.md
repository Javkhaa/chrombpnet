# Handoff — 2026-07-02 (instance being stopped)

Continuation guide for the next session. Full detail in `docs/multicell_findings.md`
(model/corpus) and `docs/cfdna_deconvolution.md §9` (cfDNA real-data).

## Where things stand

**Model / corpus**
- Corpus scaled **146 → 315 cell types**, staged at `/mnt/data/jganbat/scatac_corpus/staged/`
  (source `gs://cfdx-experiments/dna_fm/experiments/jg_experiments/scatac_corpus/staged/`;
  manifest 315 rows / 638 bigWigs). Added blood/immune (Granja/Lareau/Satpathy/Mimitou/
  Buenrostro). **No neutrophils/granulocytes** — expected gap.
- `model_315_film` trained (conditioned FiLM, 256f, amp+compile, warmup1000/cosine12000, lr5e-4).
  Converged ~step 10k, val 1387. Eval: specificity 0.837, acc counts r 0.721, nuc counts 0.605,
  AUROC 0.628, nuc profile r(σ20) 0.324/ceil 0.772. **Slightly below the 146 model** —
  undertrained per-cell (same step budget over 2× cells), not a regression. wandb `fu64exg3`.
- Nucleosome positioning plateau ~0.37 (52% of ceiling) across every lever tried.

**cfDNA (Tao 2023 / GSE186573)**
- Real plasma WGS, hg38, genuine 167 bp fragmentation, 0.28x. Ingestion works
  (`chrombpnet/cfdna/ingest.py` + `deconvolve_real.py`, entry `chrombpnet-cfdna-deconvolve`).
- **Naive deconvolution failed** (4 diagnostics → GC/mappability + wrong feature + depth).
  Reference gap now addressed by the 315 corpus; feature engineering is the remaining blocker.

## Everything is preserved
- Checkpoints + evals + cfDNA results backed up to `gs://.../scatac_corpus/model_runs/`
  (survive the `/mnt/data` mount loss on instance restart).
- Code committed + pushed on `nucleosome-head`. cfDNA test parquet re-downloadable from GCS.

## On restart (mount is lost — no fstab)
1. Re-mount `/mnt/data` (filesystem-j9, 190T). If corpus gone, it's safe in GCS `staged/`.
2. wandb key: `eval "$(grep 'export WANDB_API_KEY' ~/.zshrc | tail -1)"` for detached runs.
3. Code runs from `~/atac/chrombpnet` (its git is stale; canonical repo is `~/repos/chrombpnet`).

## Next steps (priority order)
1. **cfDNA — the TSS-dip positive control (START HERE).** Reproduce the canonical Ulz/Griffin
   TSS coverage dip with proper WPS on NC-PKU-mix15 — needs a GENCODE TSS set, **no ATAC ref**.
   Decisive gate: dip reproduces → method alive, blocker is feature engineering → build the
   Griffin GC-corrected composite pipeline against the 315 reference. No dip → per-sample
   deconvolution not viable at 0.28x; reconsider (higher-depth samples, or pooled scenarios).
2. **Eval `model_146_film_512`** (`.pt` in model_runs/) vs 256-FiLM — closes the capacity
   question for nucleosome positioning + AUROC. `chrombpnet-eval-multicell --smooth-sigma 20`.
3. **(Optional) Retrain `model_315_film` ~2× longer** (raise `--lr-decay-steps`) to match/beat
   the 146 model, if a strong 315 model is wanted for imputation (Role b).

## Key commands
- Train 315 (longer): the recipe in `scratchpad/train_315.sh` with bigger `--lr-decay-steps`.
- cfDNA deconvolve: `chrombpnet-cfdna-deconvolve -m <manifest> --manifest-root <root>
  --cfdna <fragments.parquet> --chroms chr1 chr3 chr6 -o out.json`
- cfDNA data: `gs://prima-mente-sequencing-public/tao_2023_cell_reports_medicine/`
  (`processed_data_inhouse/DNA-seq/fragments/*.parquet`; NC=healthy, CRC/STAD=cancer).
